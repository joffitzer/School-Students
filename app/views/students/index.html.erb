<h1>This is the Students index page:</h1>
<%= link_to "Create a Student", new_student_path %>
<% @students.each do |student| %>
    <h4><%= link_to "#{student.name}", student_path(student) %></h4>
    <h4>Age: <%= student.age %></h4>
    <hr>
<%end%>
